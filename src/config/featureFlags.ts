// Central feature flags for UI visibility toggles
// Flip any flag to true to re-enable the corresponding section.

export const featureFlags = {
  /** Account Intelligence Feed on Partner home — hidden for UI simplification v2 */
  showAccountIntelligenceFeed: false,
} as const;
